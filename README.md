# C++ Test

Draws a basic blank wndow.
Possible this only works on macOS for now.

## Requirements

- CMake
- clangd for formatting

## Setup

```shell
mkdir build
cd build
cmake -DCMAKE_EXPORT_COMPILE_COMMANDS=ON ..
```

## Building & running

```shell
cd build
make #to build
make run #to run
```
